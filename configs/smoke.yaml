profile: plumbing_smoke
seed: 3
device: cpu
output_dir: runs/ripii_smoke
batch_size: 1
val_batch_size: 1
steps: 1
warmup_steps: 1
lr: 0.001
weight_decay: 0.0
input_dim: 24
latent_dim: 12
hidden_dim: 24
node_dim: 3
num_nodes: 3
num_levels: 1
num_projectors: 1
codebook_size: 4
fine_codebook_size: 4
codebook_dim: 8
graph_steps: 1
graph_topk: 2
dataset_size: 8
noise_std: 0.05
transform_scale: 0.15
transform_shift: 2
transform_dim: 4
num_classes: 4
eval_every: 1
log_every: 1
compile_model: false
amp: false
depth_target: 0.5
use_projective: true
use_graph: true
use_quantizer: true
use_action: true
use_spectral_loss: true
use_equivariance_loss: true
use_identity_loss: true
use_depth_loss: true
loss_weights:
  recon: 1.0
  equiv: 0.8
  inv: 0.8
  scale: 0.2
  proj: 0.2
  spectral: 0.1
  geom: 0.1
  vq: 1.0
  node: 0.1
  moment: 0.05
  identity: 0.05
  kl: 0.01
  depth: 0.05
