# RIPII pilot v1 amendment 1

Recorded before any model run. The first attempted command exited with status 127
because this host has no `python` executable. Replace only the interpreter token with
`python3`. All seeds, modes, configuration, metrics, thresholds, paths, and scientific
decisions in `pilot_v1.md` remain unchanged.
